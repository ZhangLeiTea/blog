1、msyqldump
  -- default-character-set=utf8
    -- 对schema没有作用，总是utf8编码，
    -- 对数据起作用
    
  -- set-charset
    -- 只能在选项中，不能在option-file中
    
  -- hex-blob
    -- 对于二进制数据很有好处
    
    
    
2、可执行注释语句
Those special comment always confuse me, after checking the doc https://dev.mysql.com/doc/refman/8.0/en/comments.html

if you add a version number after the ! character, the syntax within the comment is executed only if the MySQL version is greater than 
or equal to the specified version number. The KEY_BLOCK_SIZE keyword in the following comment is executed only by servers from 
MySQL 5.1.10 or higher:

CREATE TABLE t1(a INT, KEY (a)) /*!50110 KEY_BLOCK_SIZE=1024 */;

That is not hard to understand. In term of 50110, means 5(major)01(minor)10(revision) = run that in MySQL version >= 5.1.10 or higher
