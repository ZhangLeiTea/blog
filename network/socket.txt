connectSuccess
# 成功建立连接时的处理
    # 可以立即send一个包，也可以什么都不做，何时发包由用户控制，仅维持心跳
connectFail
# 建立连接失败时的处理。例如可以尝试再次建立连接
Recv:
# socket 收到数据时的处理。例如，将数据保存到用户态的固定位置
Disconnected:
# 连接断开时的处理。不论是主动断开连接还是超时、错误导致的断开连接
OnCommand:
# socket用户传输特定的协议，此时已经是在写应用层协议了
# 根据特定的协议格式，判定是哪种协议，接收完协议的所有数据后以协议的格式触发